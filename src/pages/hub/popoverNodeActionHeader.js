import React from 'react'
import classnames from "classnames";

import {
  Col,
  Container,
  Nav,
  NavItem,
  NavLink,
  Row,
  TabContent,
  TabPane
} from "reactstrap";

import {DatePicker,Divider,Form,Input,Item,Badge,Button,Icon,Radio,Slider,Select} from 'antd'

import { Home, Settings, MessageSquare,Users,GitMerge } from "react-feather";

const PopoverNodeActionHeader = ({props}) => {
  return (
    <div>Put a delete here</div>
  )
}

export default PopoverNodeActionHeader 
